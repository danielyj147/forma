# Secrets — never commit
.env
.env.*
!.env.example
*.pem
.dev.vars
.dev.vars.*

# Node
node_modules/
dist/
.wrangler/
*.tsbuildinfo

# Account-specific provisioning state + generated deploy configs (npm run setup)
.forma/
.wrangler.*.jsonc

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Ingestion artifacts (large, reproducible; corpus PDFs re-fetchable via scripts/fetch-corpus.sh)
data/processed/
data/cache/
data/pdfs/

# Eval scratch (committed results live in evals/results/)
evals/.deepeval/
.deepeval/
.deepeval_telemetry.txt

# OS / editor
.DS_Store
*.log
